# Static assets

Place your **KhidmaMart logo** here as `logo.png` (or `logo.svg`) to use it in the navbar.

- **logo.png** — used by the app at `/logo.png`. Recommended height ~40px for navbar; the navbar constrains it to `h-10` (2.5rem).
- If the file is missing, the navbar shows a text fallback (KhidmaMart).
