# API base URL (used at build time).
# Local dev: leave empty or omit — Vite proxy sends /api to backend.
# Production: set to your backend URL, e.g. https://khidma-api.onrender.com/api/v1
VITE_API_BASE_URL=
